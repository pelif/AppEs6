export class Mask {

    constructor() { }

    formatarNome(Elem) {

        console.log(Elem;
        // let NomeValue = $(elem).val();
        // console.log(NomeValue);
        // return;
        // NomeValue = NomeValue.replace(/\d/g, "");
        // $(elem).value(NomeValue);

    }

}
